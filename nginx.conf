http {
    server {
        listen *:80;
        listen [::]:80;
        root var/www/html;
        index index.html;
        
        location /images/ {
            autoindex on;
            root var/www/images;
        }
    }
}